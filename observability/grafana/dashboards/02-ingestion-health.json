{
  "uid": "ingestion-health",
  "title": "Ingestion Health",
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "tags": [
    "oilfield",
    "observability"
  ],
  "panels": [
    {
      "id": 3,
      "type": "timeseries",
      "title": "Ingestion rows/sec (valid)",
      "datasource": {
        "type": "prometheus",
        "uid": "Prometheus"
      },
      "targets": [
        {
          "expr": "sum(rate(ingestion_rows_total{status=\"valid\"}[5m]))",
          "refId": "A",
          "legendFormat": ""
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ops"
        },
        "overrides": []
      },
      "options": {}
    },
    {
      "id": 4,
      "type": "timeseries",
      "title": "Ingestion rows/sec (invalid)",
      "datasource": {
        "type": "prometheus",
        "uid": "Prometheus"
      },
      "targets": [
        {
          "expr": "sum(rate(ingestion_rows_total{status=\"invalid\"}[5m]))",
          "refId": "A",
          "legendFormat": ""
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ops"
        },
        "overrides": []
      },
      "options": {}
    }
  ],
  "time": {
    "from": "now-1h",
    "to": "now"
  }
}