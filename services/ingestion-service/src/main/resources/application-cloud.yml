spring:
  jpa:
    show-sql: false

management:
  otlp:
    metrics:
      export:
        enabled: true
        url: ${METRICS_OTLP_URL:http://localhost:4318/v1/metrics}
        step: 30s
        headers:
          Authorization: ${METRICS_OTLP_AUTHORIZATION:}

logging:
  level:
    root: INFO
